<?xml version='1.0' encoding='utf-8'?>
<question xmlns='http://www.gradians.com'>
  <statement>
    <tex>\text{Using elementary transformations, find the}\\
\text{inverse of the matrix A, if it exists.}\\
A=\left[\begin{array}{rr}
1 &amp; 3 \\ 
2 &amp; 7\end{array} \right]</tex>
  </statement>
  <step>
    <context>\text{To find }A^{-1}\text{ using elementary transformation,}\\ 
\text{how does one express A in terms of A and I,}\\ 
\text{where I is the } \textbf{identity matrix}\text{ ?}</context>
    <tex correct='true'>A = IA \text{ (when using Row transformation)}\\
A = AI \text{ (when using Column transformation)}</tex>
    <tex>A = IA \text{ (when using Column transformation)}\\
A = AI \text{ (when using Row transformation)}</tex>
    <reason>\text{When using }\textbf{row } \text{transformation, I must be }\\ 
\text{the }\textbf{first}\text{ multiplier, whereas when using}\\
\textbf{column}\text{ transformation, it is the }\textbf{ second.}
</reason>
  </step>
  <step>
    <context>\text{What would the equation look like if you}\\
\text{apply the following transformation}\\
R_2\rightarrow &amp;R_2- 2R_1</context>
    <tex correct='true'>\left[\begin{array}{rr}
 1 &amp; 3 \\ 
0 &amp; 1\end{array} \right] =
\left[\begin{array}{rr}
 1 &amp; 0 \\ 
-2 &amp; 1\end{array} \right]A</tex>
    <tex>\left[\begin{array}{rr}
 1 &amp; 3 \\ 
0 &amp; 1\end{array} \right] =
\left[\begin{array}{rr}
1 &amp; 0 \\ 
0 &amp; 1\end{array} \right]A</tex>
    <reason>\text{Each row transformation needs to be applied}\\
\text{simultaneously to LHS and the first matrix}\\
\text{on the RHS.}</reason>
  </step>
  <step>
    <context>\text{Does the following transformation convert}\\
\mathbf{a_{12}}\text{ to }0?</context>
    <tex correct='true'>\begin{align}
R_1\rightarrow &amp;R_1 - 3R_2
\end{align}</tex>
    <reason>\text{The above transformation converts \mathbf{a_{12}}\text{ to }0\\
\text{Once all transformations so far are applied,}\\
\text{identity matrix on RHS, it becomes }A^{-1}.
</reason>
  </step>
  <choices>
    <tex correct='true'>A^{-1}=
\left[\begin{array}{rr}
7 &amp; -3 \\ 
-2 &amp; 1 \end{array} \right]</tex>
    <tex>A^{-1}\text{ does not exist.}
</tex>
    <tex>A^{-1}=
\left[\begin{array}{rr}
7 &amp; -2 \\ 
-3 &amp; 1 \end{array} \right]</tex>
    <tex>A^{-1}=
\left[\begin{array}{rr}
1 &amp; 0 \\ 
0 &amp; 1 \end{array} \right]</tex>
  </choices>
</question>
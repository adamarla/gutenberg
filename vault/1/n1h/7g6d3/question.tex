
\renewcommand\vw{5}
\renewcommand\vr{3} 
\renewcommand\vd{15}

\ifnumequal{\value{rolldice}}{0}{
  \renewcommand\vs{9}
  \renewcommand\vr{4}
  \renewcommand\vx{12}
  \renewcommand\vm{}
}{
  \ifnumequal{\value{rolldice}}{1}{
    \renewcommand\vs{8}
    \renewcommand\vx{6}
    \renewcommand\vm{\dfrac{47}{1500}}
  }{
    \ifnumequal{\value{rolldice}}{2}{
      \renewcommand\vs{10}
      \renewcommand\vx{7.5}
      \renewcommand\vm{}
    }{
      \renewcommand\vs{9}
      \renewcommand\vx{6.75}
      \renewcommand\vm{}
    }
  }
}

\SQUARE\vs\vy
\SUBTRACT\vd\vx\vz

\question[3] A torpedo boat is anchored $\vs$ km away from the nearest point $(A)$
on a shore. A messenger must be sent from the torpedo boat to a camp that is 
$\vd$ km away from $A$ along the shore. If a messenger
can walk at $\vw$ km per hour and row at $\vr$ km per hour, then where on the shore must
he land in order to reach the camp in the shortest possible time? 

\watchout

\ifprintanswers
  % stuff to be shown only in the answer key - like explanatory margin figures
  \begin{marginfigure}
  	\figinit{pt}
      \figpt 100: (0,0)
      \figpt 101: (80,0)
      \figpt 102: (0,120)
      \figpt 103: (80,120)
      \figpt 104: (20,20) % boat 
      \figpt 105: (80,20) % nearest point
      \figpt 106: (80,110) % camp
      \figpt 107: (80,80) % landing spot
      \figpt 200: (50,17)
      \figpt 201: (82,50)
      \figpt 202: (82,95)
  	\figdrawbegin{}
      \figdrawline [100,102]
      \figdrawline [101,103]
      \figdrawline [104,105,106]
      \figdrawline [104,107]
      \figdrawline [104,106]
  	\figdrawend
  	\figvisu{\figBoxA}{}{
      \fontfamily{cmss}\selectfont\normalsize
      \figwrites 104:$\textbf{Boat}$(2)
      \figwritee 105:$\textbf{Nearest Pt (A)}$(2)
      \figwritee 106:$\textbf{Camp}$(2)
      \figwritee 107:$\textbf{Landing Pt}$(2)
      \figwrites 200:$\vs\text{ km}$(1)
      \figwritee 201:$x\text{ km}$(1)
      \figwritee 202:$\vd-x\text{ km}$(1)
  	}
  	\centerline{\box\figBoxA}
  \end{marginfigure}
\fi 

\begin{solution}[\fullpage]
  The situation is as shown in the figure alongside. 

 \textbf{Step \#1: Find the extreme point(s)} 

 If $T$ be the total time it takes row to the landing point  
 and then walk from the landing point to the camp, then
   \begin{align}
       T &= \dfrac{\sqrt{(\vs^2+x^2)}}{\vr} + \dfrac{\vd-x}{\vw} \\
       \implies \dfrac{\ud T}{\ud x} &= \left( \dfrac{1}{\vr}\cdot\dfrac{1}{2}\cdot\dfrac{2x}{\sqrt{x^2+\vy}}\right) - \dfrac{1}{\vw} \\
       \implies \dfrac{\ud T}{\ud x} &= 0\text{ when } \dfrac{x}{\sqrt{x^2+\vy}} = \dfrac{\vr}{\vw}
       \text{ or } x = \vx\text{ km} 
   \end{align}
   \textbf{Step \#2: Is it a maxima or a minima?}
   
   To answer that, we need to find the second derivative.
   \begin{align}
     \dfrac{\ud^2 T}{\ud x^2} &= \dfrac{1}{\vr\cdot(x^2+\vy)^{\frac{1}{2}}} 
                 - \dfrac{x}{\vr}\times\dfrac{1}{2}\times\dfrac{1}{(x^2+\vy)^{\frac{3}{2}}} \\
                         &= \dfrac{1}{\vr\cdot(x^2+\vy)^{\frac{3}{2}}}\times(x^2-x+\vy) \\
     \left[\dfrac{\ud^2 T}{\ud x^2}\right]_{x=\vx} &> \underbrace{0\implies\text{ minima }}_{\vx^2-\vx + \vy > 0}
   \end{align}
   Hence, the messenger should land 
    \[ \vx\text{ km from } A\implies\vz\text{ km from the camp} \] 
\end{solution}
\ifprintanswers\begin{codex}$\vz$ km from the camp\end{codex}\fi

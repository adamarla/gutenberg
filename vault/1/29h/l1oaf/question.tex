


\ifnumequal{\value{rolldice}}{0}{
  % variables 
  \renewcommand{\va}{5} % # boys
  \renewcommand{\vb}{3} % # girls
  \renewcommand{\vc}{boys } % remaining
  \renewcommand{\vd}{girls }
  \renewcommand{\ve}{1440}
}{
  \ifnumequal{\value{rolldice}}{1}{
    % variables 
    \renewcommand{\va}{3}
    \renewcommand{\vb}{6}
    \renewcommand{\vc}{girls }
    \renewcommand{\vd}{boys }
    \renewcommand{\ve}{8640}
  }{
    \ifnumequal{\value{rolldice}}{2}{
      % variables 
      \renewcommand{\va}{6}
      \renewcommand{\vb}{4}
      \renewcommand{\vc}{boys }
      \renewcommand{\vd}{girls }
      \renewcommand{\ve}{34,560}
    }{
      % variables 
      \renewcommand{\va}{3}
      \renewcommand{\vb}{7}
      \renewcommand{\vc}{girls }
      \renewcommand{\vd}{boys }
      \renewcommand{\ve}{60,480}
    }
  }
}

\ifthenelse{\va > \vb}{
  \SUBTRACT\va\vb\vr
  \ADD\vb{0}\vn
  \ADD\va{0}\vm
}{
  \SUBTRACT\vb\va\vr % vr = remaining boys/girls
  \ADD\va{0}\vn % vn = min(va,vb)
  \ADD\vb{0}\vm % vm = max(va,vb)
}

%\ifthenelse{\va > \vb}{\gcalcexpr[0]\nremaining{\va - \vb}}{\gcalcexpr[0]\nremaining{\vb - \va}}
%\ifthenelse{\va > \vb}{\gcalcexpr[0]\nmin\vb}{\gcalcexpr[0]\nmin\va}
%\ifthenelse{\va > \vb}{\gcalcexpr[0]\nmax\va}{\gcalcexpr[0]\nmax\vb}

\question[2] $\va$ boys and $\vb$ girls need to stand in a line so that, as far as possible, they alternate 
and when they can't, the remaining students line up randomly. In how many ways can they stand?

\watchout

\begin{solution}[\mcq]
  As the number of \vc is more than the number of \vd, they can alternate only till such time 
  that there are \vd remaining to join the line. 
	
  Hence, we first pick the $\vn$ \vc to alternate with the $\vn$ \vd. Note also that 
  for each such combination of $\vn$ girls and $\vn$ boys, the line can start with either a 
  boy or a girl. 
  
  Then, we permute the remaining $\vr$ \vc.
  
  Given this, the required number of ways is 
  \[ N = \overbrace{2\times\underbrace{\binom\vm\vn\cdot\vn !}_{\vn\text{ \vc }}\cdot\vn !}^{\vn\text{ girls, }\vn\text{ boys }}
  \times\underbrace{\vr !}_{\text{remaining \vc}} = \ve \]

\end{solution}

\ifprintanswers
  \begin{codex}
    $\ve$
  \end{codex}
\fi

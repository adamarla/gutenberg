


\ifnumequal{\value{rolldice}}{0}{
}{
  \ifnumequal{\value{rolldice}}{1}{
  }{
    \ifnumequal{\value{rolldice}}{2}{
    }{
    }
  }
}

\question Consider the following transactions:
\begin{align}
  T1: read(A,t); t:=t+2; write(A,t); read(B,t); t:=t*3; write(B,t) \nonumber \\
  T2: read(B,s); s:=s*2; write(B,s); read(A,s); s:=s+3; write(A,s) \nonumber
\end{align}

\begin{parts}
  \part[3] Give an example of a schedule (interleaved transactions) which gives the same 
  outcome as a serial schedule but is not conflict serializable (justify your answer)

\begin{solution}[\halfpage]
  \end{solution}

  \part[3] Give an example of a schedule (interleaved transactions) which results in 
  database inconsistency (non-serializable) – justify your answer

\begin{solution}[\halfpage]
  \end{solution}


\end{parts}


\ifnumequal{\value{rolldice}}{0}{
  % variables 
  \renewcommand{\vbone}{5}
  \renewcommand{\vbtwo}{2}
  \renewcommand{\vbthree}{3}
  \renewcommand{\vbfour}{240}
  \renewcommand{\vbfive}{720}
  \renewcommand{\vbsix}{1080}
  \renewcommand{\vbseven}{40}
  \renewcommand{\vbeight}{\dfrac{6400}{3}}
}{
  \ifnumequal{\value{rolldice}}{1}{
    % variables 
    \renewcommand{\vbone}{5}
    \renewcommand{\vbtwo}{1}
    \renewcommand{\vbthree}{3}
    \renewcommand{\vbfour}{15}
    \renewcommand{\vbfive}{90}
    \renewcommand{\vbsix}{270}
    \renewcommand{\vbseven}{\dfrac{5}{4}}
    \renewcommand{\vbeight}{\dfrac{25}{12}}
  }{
    \ifnumequal{\value{rolldice}}{2}{
      % variables 
      \renewcommand{\vbone}{6}
      \renewcommand{\vbtwo}{2}
      \renewcommand{\vbthree}{3}
      \renewcommand{\vbfour}{576}
      \renewcommand{\vbfive}{2160}
      \renewcommand{\vbsix}{4320}
      \renewcommand{\vbseven}{\dfrac{384}{5}}
      \renewcommand{\vbeight}{\dfrac{384\times 96}{5}}
    }{
      % variables 
      \renewcommand{\vbone}{6}
      \renewcommand{\vbtwo}{1}
      \renewcommand{\vbthree}{3}
      \renewcommand{\vbfour}{18}
      \renewcommand{\vbfive}{135}
      \renewcommand{\vbsix}{540}
      \renewcommand{\vbseven}{\dfrac{6}{5}}
      \renewcommand{\vbeight}{\dfrac{9}{5}}
    }
  }
}
\gcalcexpr[0]{\b}{(\vbfour * \vbfour * \vbfour)/(\vbsix * 2)}

\question[4] If the $2^{nd}$, $3^{rd}$ and $4^{th}$ terms in the expansion of 
$(a+b)^n$ are $\vbfour$, $\vbfive$ and $\vbsix$ respectively, find the values
of $a$, $b$ and $n$.


\watchout

\ifprintanswers
  % stuff to be shown only in the answer key - like explanatory margin figures
\fi 

\begin{solution}[\fullpage]
  The expansion of the $r+1^{th}$ term $T_{r+1} = ^nC_r a^{n-r}b^r$, 
  therefore,
  \begin{align}
    T_2 = ^nC_1 a^{n-1} b   &= \vbfour \\
    T_3 = ^nC_2 a^{n-2} b^2 &= \vbfive \\
    T_4 = ^nC_3 a^{n-3} b^3 &= \vbsix
  \end{align}
  Take square of equation (1) and divide by equation (2),
  \begin{align}
    \dfrac{n}{n-1}\cdot a^n = \vbseven
  \end{align}
  Take cube of equation (1) and divide by equation (3),
  \begin{align}
    \dfrac{n^2}{(n-1)(n-2)}\cdot a^{2n} = \vbeight
  \end{align}
  Take square of equation (4) and divide it by equation (5)
  to eliminate $a$ and get the value of $n$,
  \begin{align}
    \dfrac{n-2}{n-1} &= \dfrac{({\vbseven})^2}{\vbeight} \\
                   n &= \vbone
  \end{align}
  Substitute $n$ in equation (4) to get $a$,
  \begin{align}
    \dfrac{\vbone}{\vbone -1} a^n &= \vbseven \\
                              a   &= \vbtwo 
  \end{align}
  Substitute $a$ and $n$ in equation (1) to get $b$,
  \begin{align}
      ^5C_1 \vbtwo^{5-1} b &= \vbfour \\
                         b &= \vbthree
  \end{align}
\end{solution}

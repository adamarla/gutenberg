
\ifnumequal{\value{rolldice}}{0}{
  \renewcommand\va{19}
  \renewcommand\vb{10}
  \renewcommand\vc{2}
  \renewcommand\vd{18}
  \renewcommand\ve{13}
  \renewcommand\vf{38}
  \renewcommand\vu{14}
  \renewcommand\vv{6}
  \renewcommand\vw{14}
  \renewcommand\vx{20}
  \renewcommand\vy{17}
  \renewcommand\vz{29}
}{
  \ifnumequal{\value{rolldice}}{1}{
    \renewcommand\va{15}
    \renewcommand\vb{20}
    \renewcommand\vc{14}
    \renewcommand\vd{8}
    \renewcommand\ve{15}
    \renewcommand\vf{28}
    \renewcommand\vu{16}
    \renewcommand\vv{14}
    \renewcommand\vw{17}
    \renewcommand\vx{12}
    \renewcommand\vy{12}
    \renewcommand\vz{29}
  }{
    \ifnumequal{\value{rolldice}}{2}{
      \renewcommand\va{17}
      \renewcommand\vb{8}
      \renewcommand\vc{18}
      \renewcommand\vd{18}
      \renewcommand\ve{9}
      \renewcommand\vf{30}
      \renewcommand\vu{2}
      \renewcommand\vv{10}
      \renewcommand\vw{13}
      \renewcommand\vx{10}
      \renewcommand\vy{0}
      \renewcommand\vz{65}
    }{
      \renewcommand\va{5}
      \renewcommand\vb{4}
      \renewcommand\vc{5}
      \renewcommand\vd{18}
      \renewcommand\ve{19}
      \renewcommand\vf{49}
      \renewcommand\vu{10}
      \renewcommand\vv{10}
      \renewcommand\vw{11}
      \renewcommand\vx{16}
      \renewcommand\vy{14}
      \renewcommand\vz{39}
    }
  }
}

\question[5]
  My friend Fred has two urns - A and B - each containing colored balls. Urn A
  has $\va\%$ brown, $\vb\%$ yellow, $\vc\%$ red, $\vd\%$ green, $\ve\%$ orange and $\vf\%$ tan 
  colored balls. Urn B has $\vu\%$ brown, $\vv$\% yellow, $\vw$\% red, $\vx\%$ green,
  $\vy\%$ orange and $\vz\%$ blue balls. 
  
  Fred draws one ball each from each of the urns. They turn out to be yellow and
  green. What is the probability that the yellow ball was from Urn A?

\watchout

\ifprintanswers
  % stuff to be shown only in the answer key - like explanatory margin figures
    \begin{tabular}[t]{rcc}
       \toprule
       & A & B \\
       \midrule
       Brown & $\va\%$ & $\vu\%$ \\
       Yellow & $\vb\%$ & $\vv\%$ \\
       Red & $\vc\%$ & $\vw\%$ \\
       Green & $\vd\%$ & $\vx\%$ \\
       Orange & $\ve\%$ & $\vy\%$ \\ 
       Tan & $\vf\%$ & 0 \\
       Blue & 0 & $\vz\%$\\
       \bottomrule
    \end{tabular}
\fi 

\FRACTIONSIMPLIFY\vd{100}\a\b % G_A
\FRACTIONSIMPLIFY\vx{100}\c\d % G_B
\FRACTIONSIMPLIFY\vb{100}\e\f % Y_A
\FRACTIONSIMPLIFY\vv{100}\g\h % Y_B
\FRACMULT\c\d\e\f\p\q
\FRACMULT\a\b\g\h\r\s
\FRACDIV\r\s\p\q\m\n
\FRACADD\m\n{1}{1}\vj\vk
\FRACDIV{1}{1}\vj\vk\vl\vm

\begin{solution}[\fullpage]
   If $Y$ be the event that a yellow ball is drawn and $G$ the event that a green ball is 
   drawn, then the probability we seek is $P(Y_A \vert YG)$, where $YG$ is the event
   that a yellow and a green ball are drawn and $Y_A$ the event that the yellow ball is 
   drawn from Urn A
   
   \begin{align}
      P(Y_A \vert YG) &= \dfrac{P(YG \vert Y_A)\cdot P(Y_A)}{P(YG \vert Y_A)\cdot P(Y_A) + P(YG \vert Y_B)\cdot P(Y_B)}
   \end{align}
   
   Now, $P(YG \vert Y_A) = P(G_B)$. Why? Because the only way to get a green and a yellow ball 
   \textit{given that} the yellow ball is drawn from Urn A is to draw a green ball from Urn B.
   
   Similarly, $P(YG \vert Y_B) = P(G_A)$. And therefore, 
   
   \begin{align}
       P(Y_A \vert YG) &= \dfrac{P(G_B)\cdot P(Y_A)}{P(G_B)\cdot P(Y_A) + P(G_A)\cdot P(Y_B)} \\
                       &= \dfrac{\frac\vx{100}\cdot\frac\vb{100}}{\frac\vx{100}\cdot\frac\vb{100}+\frac\vd{100}\cdot\frac\vv{100}} 
                       = \dfrac\vl\vm 
   \end{align}
\end{solution}

\ifprintanswers
  \begin{codex}
    $P_{\text{reqd}} = \dfrac\vl\vm$
  \end{codex}
\fi




\ifnumequal{\value{rolldice}}{0}{
  % variables 
  \renewcommand{\va}{2695}
  \renewcommand{\vb}{(2)}
  \renewcommand\vc{(2,5)}
  \renewcommand\vd{(2,5,6,9)}
  \renewcommand\ve{(2,5,6,9)}
  \renewcommand\vp{32}
  \renewcommand\vf{(2)}
  \renewcommand\vg{(6)}
  \renewcommand\vh{(2,5,6)}
  \renewcommand\vi{(2,5,6,9)}
  \renewcommand\vq{12}
  \renewcommand\vj{(2)}
  \renewcommand\vk{(6)}
  \renewcommand\vl{(9)}
  \renewcommand\vm{(2)}
  \renewcommand\vr{1}
}{
  \ifnumequal{\value{rolldice}}{1}{
    % variables 
    \renewcommand{\va}{5296}
    \renewcommand{\vb}{(2)}
    \renewcommand{\vc}{(2,5,6,9)}
    \renewcommand{\vd}{(2,5,6,9)}
    \renewcommand{\ve}{(2,5,6,9)}
    \renewcommand{\vf}{(5)}
    \renewcommand{\vg}{(2)}
    \renewcommand{\vh}{(2,5,6)}
    \renewcommand{\vi}{(2,5,6,9)}
    \renewcommand\vj{(5)}
    \renewcommand\vk{(2)}
    \renewcommand\vl{(9)}
    \renewcommand\vm{(2,5)}
    \renewcommand\vp{64}
    \renewcommand\vq{12}
    \renewcommand\vr{2}
  }{
    \ifnumequal{\value{rolldice}}{2}{
      % variables 
      \renewcommand{\va}{6259}
      \renewcommand{\vb}{(2,5)}
      \renewcommand{\vc}{(2,5,6,9)}
      \renewcommand{\vd}{(2,5,6,9)}
      \renewcommand{\ve}{(2,5,6,9)}
      \renewcommand{\vf}{(6)}
      \renewcommand{\vg}{(2)}
      \renewcommand{\vh}{(2)}
      \renewcommand{\vi}{(2,5,6,9)}
      \renewcommand{\vj}{(6)}
      \renewcommand\vk{(2)}
      \renewcommand\vl{(5)}
      \renewcommand\vm{(2,5,6)}
      \renewcommand\vp{128}
      \renewcommand\vq{4}
      \renewcommand\vr{3}
    }{
      % variables 
      \renewcommand{\va}{9265}
      \renewcommand{\vb}{(2,5,6)}
      \renewcommand{\vc}{(2,5,6,9)}
      \renewcommand{\vd}{(2,5,6,9)}
      \renewcommand{\ve}{(2,5,6,9)}
      \renewcommand{\vf}{(9)}
      \renewcommand{\vg}{(2)}
      \renewcommand{\vh}{(2,5)}
      \renewcommand{\vi}{(2,5,6,9)}
      \renewcommand{\vj}{(9)}
      \renewcommand\vk{(2)}
      \renewcommand\vl{(6)}
      \renewcommand\vm{(2)}
      \renewcommand\vp{192}
      \renewcommand\vq{8}
      \renewcommand\vr{1}
    }
  }
}

\ADD\vp\vq\a
\ADD\a\vr\vx
\ADD\vx{84}\vz

\question[5] How many numbers $< \va$ can be formed using some or all of the digits $2,5,6,9$ 
if the digits are allowed to repeat?

\watchout

\begin{solution}[\halfpage]
	Any one-, two- or three-digit number made using digits from $2,5,6,9$ 
	is guaranteed to be $\leq \va$. They are also easy to count
	\begin{align}
		N_{\texttt{< 4-digits}} &= N_1 + N_2 + N_3 \\ 
		 &= 4 + 4^2 + 4^3 = 84
	\end{align}
	
	It is when counting the 4-digit numbers that one has to be careful. 
  
  Refer to the table below.

	\begin{tabular}{ccccc}
		\toprule
		Thousand & Hundreds & Tens & Units & \# possibilities \\
		\midrule
      $\vb$ & $\vc$ & $\vd$ & $\ve$ & $\vp$ \\
      $\vf$ & $\vg$ & $\vh$ & $\vi$ & $\vq$ \\
      $\vj$ & $\vk$ & $\vl$ & $\vm$ & $\vr$ \\
    \bottomrule
	\end{tabular}

  Hence, the number of numbers $<\va$ that can be formed is $\vx + 84 = \vz$.	
\end{solution}

\ifprintanswers
  \begin{codex}
    $\vz$
  \end{codex}
\fi

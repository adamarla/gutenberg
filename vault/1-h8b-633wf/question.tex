%\noprintanswers
%\setcounter{rolldice}{3}
%\printrubric

\ifnumequal{\value{rolldice}}{0}{
  % variables 
  \renewcommand{\vbone}{9}
  \renewcommand{\vbtwo}{5}
  \renewcommand{\vbthree}{(3,6),(4,5),(5,4),(6,3)}
  \renewcommand{\vbfour}{4}
  \renewcommand{\vbfive}{(1,4),(2,3),(3,2),(4,1)}
  \renewcommand{\vbsix}{4}
}{
  \ifnumequal{\value{rolldice}}{1}{
    % variables 
    \renewcommand{\vbone}{6}
    \renewcommand{\vbtwo}{7}
    \renewcommand{\vbthree}{(1,5),(2,4),(3,3),(4,2),(5,1)} %valid ways B
    \renewcommand{\vbfour}{5}
    \renewcommand{\vbfive}{(1,6),(2,5),(3,4),(4,3),(5,2),(6,1)}
    \renewcommand{\vbsix}{6}
  }{
    \ifnumequal{\value{rolldice}}{2}{
      % variables 
      \renewcommand{\vbone}{7}
      \renewcommand{\vbtwo}{9}
      \renewcommand{\vbthree}{(1,6),(2,5),(3,4),(4,3),(5,2),(6,1)}
      \renewcommand{\vbfour}{6}
      \renewcommand{\vbfive}{(3,6),(4,5),(5,4),(6,3)}
      \renewcommand{\vbsix}{4}
    }{
      % variables 
      \renewcommand{\vbone}{5}
      \renewcommand{\vbtwo}{9}
      \renewcommand{\vbthree}{(1,4),(2,3),(3,2),(4,1)}
      \renewcommand{\vbfour}{4}
      \renewcommand{\vbfive}{(3,6),(4,5),(5,4),(6,3)}
      \renewcommand{\vbsix}{4}
    }
  }
}

\FRACMINUS{1}{1}\vbfour{36}\p\q % A'
\FRACMINUS{1}{1}\vbsix{36}\r\s % B'
\FRACMULT\p\q\r\s\m\n
\FRACMULT\m\n\vbfour{36}\a\b
% \MULTIPLY\vbfour\m\a

\question[3] $A$ and $B$ take turns to throw a pair of dice. $A$ wins if he gets a 
total of $\vbone$ in a throw. $B$ wins if she gets a total of $\vbtwo$. 
The game ends as soon as one person wins. If $A$  starts the game, what is the probability
that $A$ wins in the third throw (over $A$ and $B$) of the pair of dice?

\insertQR[10pt]{qrc}

\watchout[-40pt]

\ifprintanswers
\fi 

\begin{solution}[\halfpage]
  If $W_A$ be the event that $A$ gets a $\vbone$ in a given roll of dice and therefore wins, 
  then $W_A$ happens if values on the dice are in $\lbrace \vbthree \rbrace\Rightarrow\vbfour$ ways 

  Similarly, if $W_B$ be the event that $B$ wins after rolling the dice, then 
  $B$ can happen if $(x,y)\in\lbrace\vbfive\rbrace\Rightarrow\vbsix$ ways

  Therefore, the probability of $A$ winning in the third throw is
  \begin{align}
    P(W_A'\bigcap W_B'\bigcap W_A) &= P(W_A')\cdot P(W_B')\cdot P(W_C') \\
      &= \left( 1-\WRITEFRAC\vbfour{36}\right)\cdot\left(1-\WRITEFRAC\vbsix{36}\right)\cdot\WRITEFRAC\vbfour{36} \\
      &= \WRITEFRAC\p\q\cdot\WRITEFRAC\r\s\cdot\WRITEFRAC\vbfour{36} = \dfrac\a\b
  \end{align}
\end{solution}

\ifprintrubric
  \begin{table}
  	\begin{tabular}{ p{5cm}p{5cm} }
  		\toprule % in brief (4-6 words), what should a grader be looking for for insights & formulations
  		  \sc{\textcolor{blue}{Insight}} & \sc{\textcolor{blue}{Formulation}} \\ 
  		\midrule % ***** Insights & formulations ******
        Individual rolls by $A$ and $B$ are independent & Correctly listed favourable outcomes \\
  		\toprule % final numerical answers for the various versions
        \sc{\textcolor{blue}{If question has $\ldots$}} & \sc{\textcolor{blue}{Final answer}} \\
  		\midrule % ***** Numerical answers (below) **********
        $A$ wins on $9$ & $\dfrac{64}{729}$ \\ 
        $A$ wins on $6$ & $\qquad\dfrac{775}{7776}$ \\ 
        $A$ wins on $7$ & $\dfrac{10}{81}$ \\ 
        $A$ wins on $5$ & $\qquad\dfrac{64}{729}$ \\ 
  		\bottomrule
  	\end{tabular}
  \end{table}
\fi
